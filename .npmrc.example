# Copy to `.npmrc` locally (do not commit tokens). Bankrate internal docs have the
# exact registry host and how to obtain an auth token for the `@bankrate` scope.
#
# Example pattern (replace placeholders):
# @bankrate:registry=https://YOUR_REGISTRY_HOST/
# //YOUR_REGISTRY_HOST/:_authToken=${NPM_TOKEN}
