# Copy to `.npmrc` locally (gitignored). Requires a GitHub token with read:packages.
#
#   cp .npmrc.example .npmrc
#
# Authenticate: export GITHUB_TOKEN=ghp_...  (or use `gh auth token`)
#
@bankrate:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
