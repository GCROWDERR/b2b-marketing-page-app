const base = "/marketing/brush";

/** Brush stroke backgrounds from brand-identity homepage hero. */
export const MARKETING_BRUSH_IMG = {
  desktop: `${base}/brush-background-desktop.png`,
  mobile: `${base}/brush-background-mobile.png`,
  /** Light gray section edges — brand homepage Why Bankrate / logo band. */
  sectionTop: `${base}/section-top.svg`,
  sectionBottom: `${base}/section-bottom.svg`,
} as const;
