/** Shared body / accent copy classes from brand-identity homepage. */

export const marketingHeroBody = "text-lg leading-[1.7] text-gray-50";

export const marketingSectionLead =
  "text-lg leading-relaxed text-gray-700 text-balance";

export const marketingSectionLeadCenter =
  "text-lg leading-relaxed text-gray-700 text-balance text-center";

export const marketingBody = "text-gray-700 tracking-tighter leading-relaxed";

export const marketingBodySm = "text-sm leading-relaxed text-gray-700";

export const marketingCardIcon = "size-full text-gray-800";

export const marketingOnDarkBody = "text-lg leading-relaxed text-gray-50";

/**
 * Section eyebrows on light backgrounds — brand homepage uses gray-600
 * (editorial, member-experience, methodology), not primary blue.
 */
export const marketingEyebrowSection = "text-gray-600";

/** Large section divider — e.g. homepage "How it works" (EyebrowLg + gray-600). */
export const marketingEyebrowSectionLg = "text-gray-600";

/** Hero / banner eyebrows on navy — brand banners use blue-300. */
export const marketingEyebrowHero = "text-blue-300";

/**
 * Card kicker / subtitle — homepage next-steps CardDescription pattern
 * (sentence-case, blue-800, not uppercase eyebrow).
 */
export const marketingCardKicker = "text-lg tracking-tighter text-blue-800";

/**
 * Card footer CTA — brand homepage next-steps uses size="lg" but keeps text-sm
 * (B2B t-button lg bumps to text-lg; override to match brand).
 */
export const marketingCardCtaButton =
  "min-h-12 px-6 text-sm font-semibold tracking-tight";
